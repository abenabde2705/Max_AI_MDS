services:
  postgres:
    image: postgres:15
    container_name: maxai-postgres
    environment:
     POSTGRES_USER: postgres
     POSTGRES_PASSWORD: password
     POSTGRES_DB: maxai
    volumes:
     - postgres_data:/var/lib/postgresql/data
    healthcheck:
     test: ["CMD-SHELL", "pg_isready -U postgres"]
     interval: 10s
     timeout: 5s
     retries: 5
    networks:
     - web
     - default
  backend:
    build: ./server-app
    container_name: maxai-backend
    env_file: ./.env
    depends_on:
      postgres:
        condition: service_healthy
    command: node dist/server.js
    environment:
      - DB_HOST=postgres
    networks:
      - web
      - default

  web:
    build: ./frontend-react
    container_name: maxai-frontend-react
    env_file: ./.env
    command: npm run dev -- --host
    depends_on: [ backend ]
    networks:
      - web
      - default
    ports:
      - "5173:5173"

  # Profil "local-ai" : uniquement si tu veux tester chat_api/ollama en local
  # En dev normal, ces services tournent sur le VPS → ne pas les démarrer ici
  ollama:
    profiles: [ "local-ai" ]
    image: ollama/ollama
    container_name: maxai-ollama
    restart: always
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - default

  chat_api:
    profiles: [ "local-ai" ]
    build:
      context: ./chat_api
    container_name: maxai-chat-api
    env_file: ./.env
    restart: always
    environment:
      - OLLAMA_HOST=http://ollama:11434
    depends_on:
      - ollama
    networks:
      - web
      - default

volumes:
  postgres_data:
  ollama_data:

networks:
  web:
    external: true
