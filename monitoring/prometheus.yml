global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"

scrape_configs:
  - job_name: 'max-ai-backend'
    static_configs:
      - targets: ['172.17.0.1:3000']  # IP du host Docker sous Linux
    scrape_interval: 5s
    metrics_path: /metrics